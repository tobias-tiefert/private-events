<%= form_with  model: @invitation do |form| %>
  <%= form.hidden_field :event_id, value: @event.id %>
  <%= form.button "Attend", class: "button button--primary event-detail__attend-button"  %>
  <%= form.submit "Attend"  %>
<% end %>